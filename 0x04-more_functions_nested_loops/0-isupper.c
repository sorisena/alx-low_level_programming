#include "main.h"

/**
 *_isupper - print upper and lower case.
 *@x:input parameter
 * Return: 1 for upper case and 0 for lower case.
 */

int _isupper(int x)

{
	if (x >= 65 && x <= 90)
	{ return (1);
	}
	return (0);
}
